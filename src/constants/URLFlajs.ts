export const getURLFlag = (code: string): string =>
	`https://flagcdn.com/48x36/${code.toLowerCase()}.png`;
// export const getURLFlag = (code: string): string =>`https://openweathermap.org/images/flags/${code.toLowerCase()}.png`;
// https://flagcdn.com/48x36/jp.png
